# ============================================================
# .github/workflows/ci-lean.yml (Continuous Integration)
# ============================================================
# Updated: 2026-05-31 THEORY VARIANT, ONLY BUILD

name: CI Lean

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  lean:
    name: Build Lean and export contract
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      # ============================================================
      # A) ASSEMBLE: Get code and set up Lean
      # ============================================================

      - name: A1) Checkout repository code
        uses: actions/checkout@v6

      - name: A2) Set up Lean toolchain
        uses: leanprover/lean-action@v1

      # ============================================================
      # B) BUILD: Compile Lean project
      # ============================================================

      - name: B1) Build Lean project
        run: lake build
        # WHY: Compiles the public Lean theory surface.

      - name: B2) Build Lean test surface
        run: lake build TestAll
        # WHY: Confirms the declared Lean test imports and public surface compile.
